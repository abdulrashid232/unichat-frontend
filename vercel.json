{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/unichat-frontend/browser",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}